#################################################
## TREND COMPARE ################################
#################################################

ANALYSIS$CORR <- list()
source("Functions/aw_mean.R")

# Main Climatic drivers for trend 
#  - Correlation analysis?

ANALYSIS$CORR$MAP <- list("TEMP" = list(), "PREC" = list())

for(var in c("TEMP", "PREC")){
  for(lon in 1:96){
    for(lat in 1:48){
      
    }
  }
}
